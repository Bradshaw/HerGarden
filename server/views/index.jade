extends layout

block content
    if user
        h1= title
        p Welcome, #{user.name}
        a(href="/profile") View profile
    else
        h1= title
        p Welcome to #{title}
        a(href="/users/login") Log in...